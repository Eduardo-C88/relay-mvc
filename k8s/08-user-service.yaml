apiVersion: apps/v1
kind: Deployment
metadata:
  name: user-service
  namespace: relay
spec:
  replicas: 1
  selector:
    matchLabels:
      app: user-service
  template:
    metadata:
      labels:
        app: user-service
    spec:
      # --- Wait for Postgres to be ready before starting ---
      initContainers:
      - name: wait-for-postgres
        image: busybox:1.28
        command: ['sh', '-c', "until nslookup postgres-service.relay.svc.cluster.local; do echo waiting for postgres; sleep 2; done"]
      - name: run-migrations
        image: ed18c/user-service:8d06298
        command: ["npm", "run", "migrate"]
        env:
          - name: AUTH_DATABASE_URL
            value: "postgres://postgres:postgres@postgres-service:5432/auth_db"

      containers:
        - name: user-service
          image: ed18c/user-service:8d06298
          ports:
            - containerPort: 3001
          resources:
            limits:
              memory: "256Mi"
              cpu: "500m"
            requests:
              memory: "128Mi"
              cpu: "250m"
          env:
            # JWT Secrets
            - name: JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: JWT_SECRET
            - name: REFRESH_JWT_SECRET
              valueFrom:
                secretKeyRef:
                  name: jwt-secret
                  key: REFRESH_JWT_SECRET
            
            # Database Configuration
            - name: AUTH_DATABASE_URL
              value: "postgres://postgres:postgres@postgres-service:5432/auth_db"
            
            - name: AUTH_PORT
              value: "3001"
            
            # RabbitMQ Configuration
            - name: RABBITMQ_URL
              value: "amqp://admin:admin@rabbitmq-service:5672"

          # ---Health Checks ---
          livenessProbe:
            httpGet:
              path: /health # Or whatever health endpoint your app has
              port: 3001
            initialDelaySeconds: 10
            periodSeconds: 10
---
apiVersion: v1
kind: Service
metadata:
  name: user-service
  namespace: relay
spec:
  selector:
    app: user-service
  ports:
    - protocol: TCP
      port: 3001
      targetPort: 3001
  type: ClusterIP
        