apiVersion: v1
kind: ConfigMap
metadata:
  name: postgres-resource-config
  namespace: relay
data:
  postgres-resource-db: relay_resource_db
  postgres-resource-host: postgres-resource-service
  postgres-resource-port: "5432"
